import 'package:flutter/material.dart';

class ExpansionsPage extends StatelessWidget {
  const ExpansionsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('ExpansionsPage');
  }
}
