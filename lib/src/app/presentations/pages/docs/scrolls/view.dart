import 'package:flutter/material.dart';

class ScrollsPage extends StatelessWidget {
  const ScrollsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
