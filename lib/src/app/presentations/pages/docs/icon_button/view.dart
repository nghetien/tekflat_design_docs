import 'package:flutter/material.dart';

class IconButtonPage extends StatelessWidget {
  const IconButtonPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('IconButtonPage');
  }
}
