import 'package:flutter/material.dart';

class AppRefresherPage extends StatelessWidget {
  const AppRefresherPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('AppRefresherPage');
  }
}
